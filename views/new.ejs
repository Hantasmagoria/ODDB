<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Registration</title>
    <% include ../partials/head.ejs %> <% include ../controllers/validation.ejs
    %>
  </head>
  <body>
    <header class="header-banner">
      <div class="container-width">
        <div class="logo-container">
          <div class="logo">Overdoze</div>
        </div>
        <nav class="menu">
          <div class="menu-item"><a href="/leaderboard">Leaderboard</a></div>
          <div class="menu-item"><a href="/">Game</a></div>
        </nav>
        <div class="clearfix"></div>
        <form
          class="container-register"
          name="Registration"
          action="/user"
          onsubmit="return validateForm()"
          onload="return validateForm(<%=redirect.reason%>)"
          method="POST"
        >
          username:
          <input id="usernameField" type="text" name="username" /><br />
          password:
          <input type="password" name="password" /><br />
          confirm password:
          <input type="password" name="repassword" /><br />
          e-mail: <input type="email" name="email" /><br />
          <div id="validbo" class="hide">
            <p id="blank" class="hide">
              Username, Password, and Confirm Password must be filled.
            </p>
            <p id="invalid" class="hide">
              Password and Confirm Password do not match.
            </p>
          </div>
          <input class="lead-btn" type="submit" name="" value="Sign Up" />
          <input
            class="lead-btn"
            type="button"
            value="back"
            onclick="window.location.href='/'"
          />
          <h3>
            <%=`Registered Users: ${userCount}`%>
          </h3>
          <h3>
            <%if(redirect.reason == "passwords"){%>Error: <%=redirect.reason%>
            do not match.<%}%> <%if(redirect.reason == "email"){%>Error:
            <%=redirect.body%> Already registered.<%}%> <%if(redirect.reason ==
            "username"){%>Error: Username '<%=redirect.body%>' Already
            registered.<%}%>
          </h3>
        </form>
        <div class="lead-title">GameArea</div>
        <div class="lead-btn">Start Button</div>
      </div>
    </header>
    <!-- <h1>New Player</h1>
    <form
      name="Registration"
      action="/user"
      onsubmit="return validateForm()"
      method="POST"
    >
      username: <input id="usernameField" type="text" name="username" /><br />
      password:
      <input type="password" name="password" /><br />
      confirm password:
      <input type="password" name="repassword" /><br />
      e-mail: <input type="email" name="email" /><br />
      <div id="validbo" class="hide">
        <p id="blank" class="hide">
          Username, Password, and Confirm Password must be filled.
        </p>
        <p id="invalid" class="hide">
          Password and Confirm Password do not match.
        </p>
      </div>
      <input type="submit" name="" value="Sign Up" />
    </form>
    <input type="button" value="back" onclick="window.location.href='/'" />
    <h3>Registered Users: userCount</h3> -->
  </body>
</html>
