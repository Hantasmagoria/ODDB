<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Login Screen</title>
    <% include ../partials/head.ejs %> <% include ../controllers/validation.ejs
    %>
  </head>
  <body>
    <header class="header-banner">
      <div class="container-width">
        <div class="logo-container">
          <div class="logo">Overdoze</div>
        </div>
        <nav class="menu">
          <div class="menu-item">
            <%if(loggedIn){%> <a href="/user/<%=username%>">Profile</a><%}
            else{%> <a href="/login">Login</a><%}%>
          </div>
          <div class="menu-item"><a href="/leaderboard">Leaderboard</a></div>
          <div class="menu-item"><a href="/">Game</a></div>
        </nav>
        <div class="clearfix"></div>
        <%if(!loggedIn){%>
        <form
          class="container-login"
          name="Login"
          action="/user/login"
          onsubmit="return validateForm()"
          method="POST"
        >
          username: <input type="text" name="username" /><br />
          password:
          <input type="password" name="password" /><br />
          <div id="validbo" class="hide">
            <p id="blank" class="hide">Username and Password must be filled.</p>
            <p id="invalid" class="hide">Wrong Username or Password.</p>
          </div>
          <input class="lead-btn" type="submit" value="Sign In" /><input
            class="lead-btn"
            type="button"
            value="Register"
            onclick="window.location.href='/user/new'"
          />
        </form>
        <%} else{%>
        <h1>
          Already logged in as <%=username%>. <a href="/logout">Logout?</a>
        </h1>
        <%}%>
        <div class="lead-title">GameArea</div>
        <div class="lead-btn">Start Button</div>
      </div>
    </header>
  </body>
</html>
